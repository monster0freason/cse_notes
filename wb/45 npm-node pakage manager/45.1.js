//       topic: npm(node pakage manager)

//  #149.1 (npm init) command creates a json folder with all kinds ofg info. everytime you install something it will update the pakage.json file. 

// #149.2 (npm install) creates a node_modules folder which contains all the pakages we installed

// #149.3 if we want to install a different version of a module we can do it by (npm install slugify@1.3.5-->major.minor.patch) 

// #149.4 ( npm install nodemon --save-dev),nodemon restarts the server automatically when the changes are made. (--save-dev) command saves the dev-dependencies folder-->it won't be used after going into production.

// #149.5 if we want use nodemon globally we can use this command(npm install nodemon --global )

// #149.6 (npm i) is shortcut for (npm install)

// #149.7 "^2.0.2" this means it will install exactly that version. "~2.0.2"this means it will install the new thing if there are new patches available.">2.0.2" will install the latest version.